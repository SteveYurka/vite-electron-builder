import React, { Fragment, useState } from 'react';
import './App.css';
import { observer } from 'mobx-react';

const App = observer(() => {
  return (
    <div className="App">
      <header className="App-header">

        <p>app header bar fdsada</p>
      </header>

    </div>
  );
});

export default App;
